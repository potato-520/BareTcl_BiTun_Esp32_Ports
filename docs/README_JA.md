# BareTcl & BiTun ESP32 移植・統合プロジェクト

本项目は、超軽量スクリプトインタプリタである **BareTcl** と、セキュアなネットワークプロキシトンネルである **BiTun** を、ESP-IDFフレームワークを用いてESP32プラットフォームに移植し、統合したネイティブC言語プロジェクトです。

本プロジェクトにより、ESP32チップ上で動作する超軽量の対話型Tclコマンドラインシェルが提供されます。GPIOなどのハードウェア制御、ネットワーク診断（Ping/Ipconfig）、不揮発性システム設定の保存（NVS）、およびセキュアなBiTun SOCKS5プロキシトンネル（KCPマルチプレクシングおよびChaCha20-Poly1305 AEAD暗号化）の動的制御などが実現します。

---

## 1. 主な機能 (Key Features)

*   **軽量Tclエンジン (BareTcl)**:
    *   外部の動的メモリ割り当て（`malloc`/`free`）への依存は一切ありません。実行環境は固定された静的メモリプール（Arena）内に完全にバインドされているため、リソース制限の厳しいマイコン環境でも極めて堅牢に動作します。
    *   ANSIカラーコードおよび非ブロッキングな文字単位の入力に対応した、インタラクティブなコンソールシェルを提供します。
    *   GPIO制御（ピンモード設定、デジタル入出力）、スリープディレイ（`sleep`）、ログ詳細度制御（`log`）、ネットワーク状態表示（`ipconfig`）、およびICMP診断（`ping`）などのカスタムコマンドを登録しています。
*   **不揮発性Key-Valueストレージ (NVSラッパー)**:
    *   ESP32の不揮発性ストレージ（NVS）へのアクセス機能を、シンプルなTclコマンドである `nvs_set` および `nvs_get` にパッケージ化しました。
    *   再起動後も、接続先のサーバー構成（リモートサーバー、ポート、事前共有キー）を保持できます。
*   **セキュアネットワークトンネル (BiTun移植)**:
    *   セキュアなUDP/KCP SOCKS5プロキシトンネルであるBiTunを移植し、ESP32/FreeRTOS向けの堅牢なOS抽象化レイヤー（OSAL）を実装しました。
    *   暗号化と検証には **ChaCha20-Poly1305 AEAD** を使用し、インプレース（原位）復号と署名チェックを行うことで、強力なパケットロス耐性と改ざん防止を実現します。
    *   グローバルなシングルスレッド非同期DNSリゾルバとeventfd駆動型の起動メカニズムにより、SRAM使用量とスケジューラのオーバーヘッドを最適化しています。
*   **Tclによる動的ライフサイクル管理**:
    *   SOCKS5プロキシの実行状態を、Tclコンソールコマンド `bitun_start` および `bitun_stop` でバックグラウンドから動的に制御できます。
    *   起動時に最新のサーバー設定とキーをNVSから直接取得するため、ファームウェアの再コンパイルや再書き込みを行うことなく、リモート接続先を動的に変更できます。

---

## 2. ディレクトリ構成 (Project Directory Structure)

```text
├── assets/
│   ├── BareTcl/          # BareTcl公式リポジトリのサブモジュール (assets/BareTcl)
│   └── BiTun/            # BiTun公式リポジトリのサブモジュール (assets/BiTun)
├── components/
│   └── bitun_wrapper/
│       ├── CMakeLists.txt
│       └── bitun_osal_esp32.c # BiTun向けに実装した完全なESP32/FreeRTOS OSALアダプタ
├── main/
│   ├── CMakeLists.txt
│   ├── main.c            # アプリケーションのエントリポイント、カスタムTclコマンドの登録、ブートストラップ処理
│   ├── console.html      # 原始HTML版Webコンソール画面
│   ├── esp32_lib.tcl     # ESP32用Tcl自挙ブートストラップライブラリ
│   ├── console_html.c    # 自動生成されたWebコンソール用Cバイト配列
│   └── esp32_lib.c       # 自動生成されたTcl自挙用Cバイト配列
├── tools/
│   ├── debug/            # OpenOCD / GDBデバッグ用の構成ファイル (debug.cfg, debug.svd)
│   ├── flash/            # Windows向け環境構築不要の一クリック書き込みツール (flash.bat, esptool.zip, flashcom.txt)
│   └── tcl2c_esp32.py    # Tcl/HTMLをCバイト配列へ自動変換するPythonビルドスクリプト
├── CMakeLists.txt
├── sdkconfig             # ESP-IDFビルド構成ファイル
├── build.sh              # Tclのパッケージ化とESP32ファームウェアビルドを自動実行するBashスクリプト
├── clean.sh              # ビルド生成物をクリーンアップするBashスクリプト
├── LICENSE               # Apache 2.0 オープンソースライセンス
└── README.md             # プロジェクト説明書 (簡体字中国語)
```

---

## 3. クイックスタート (Quick Start)

### 3.1 前提条件
*   ESP-IDF v5.0以上 (推奨：**ESP-IDF v5.3**)。
*   CMake および Ninja ビルドツール。
*   Python 3.x。

### 3.2 ビルド
TclスクリプトをCのバイト配列に自動変換し、ESP-IDFプロジェクトをビルドするためのシェルスクリプト `build.sh` を提供しています：

```bash
# 実行権限を付与
chmod +x build.sh

# ビルドスクリプトを実行
./build.sh
```

### 3.3 書き込み（フラッシュ）
*   **Windows環境 (一クリック書き込みを推奨)**：
    `tools/flash/` ディレクトリに移動し、`flash.bat` スクリプトをダブルクリックして直接実行します。Windows側にESP-IDFツールチェーンやPython環境をインストールすることなく、自動的にシリアルポートを検出してビルド済みのファームウェアを書き込むことができます。
*   **Linux / macOS環境 (標準のESP-IDFコマンドを使用)**：
    ビルド完了後、プロジェクトのルートディレクトリで以下のコマンドを実行し、書き込みとモニタリングを開始します（例：ESP32-C3）：
    ```bash
    idf.py -p <シリアルポート名> flash monitor
    ```

---

## 4. Tclコマンドリファレンス (Tcl Commands Manual)

対話型の Tcl Shell（シリアル端末または内蔵WebコンソールWebSocketからアクセス可能）に入ると、以下のカスタムコマンドを使用できます：

### 4.1 ハードウェアおよびシステム制御
*   **`gpio_mode <pin> <mode>`**: ピンモードを設定します。
    *   `mode`: `0` = 入力, `1` = 出力, `2` = 内部プルアップ抵抗付き入力。
*   **`digital_write <pin> <level>`**: 出力ピンに論理レベルを書き込みます。`level` は `0` または `1`。
*   **`digital_read <pin>`**: ピンの現在の論理状態を読み取ります。`0` または `1` を返します。
*   **`sleep <ms>`**: 指定されたミリ秒間、タスクの実行を一時停止します（内部的にFreeRTOSタスクをイールドします）。
*   **`log <on|off>`**: バックグラウンドのWi-Fiおよびシステムログの表示/非表示を切り替え、Tclコンソールの可読性を維持します。

### 4.2 ネットワーク診断
*   **`ipconfig`**: 現在のWi-Fi接続ステータス、ローカルIPアドレス、サブネットマスク、およびゲートウェイを表示します。
*   **`ping <host>`**: 対象のIPまたはドメイン名にICMP Echo要求を送信し、往復時間（RTT）の結果を表示します。

### 4.3 不揮発性ストレージ (NVS)
*   **`nvs_set <key> <value>`**: NVSに文字列のキーと値のペアを保存します。
    *   *注意*: `key` の長さは最大 15 バイトです。
*   **`nvs_get <key> ?default?`**: NVSから文字列を取得します。
    *   指定したキーが存在しない場合、オプショナル引数 `default` を返します（省略した場合は空文字列 `""`）。

### 4.4 BiTunトンネルライフサイクル制御
*   **`bitun_start`**: バックグラウンドで非同期にBiTunプロキシトンネルを起動します。
    *   起動時にNVSの `"storage"` ネームスペースから以下の設定を動的に取得します（見つからない場合はデフォルト値を使用します）：
        *   `bitun_rem_ip`: リモートKCPサーバーのIPアドレスまたはドメイン名（デフォルト：`"127.0.0.1"`）。
        *   `bitun_rem_port`: リモートKCPサーバーのリスニングポート（デフォルト：`9999`）。
        *   `bitun_loc_port`: ローカルSOCKS5プロキシポート（デフォルト：`1080`）。
        *   `bitun_psk`: 32バイトの事前共有キー文字列（デフォルト：`"00000000000000000000000000000000"`）。
*   **`bitun_stop`**: バックグラウンドで実行中のBiTunトンネルを停止し、アクティブなすべてのネットワークソケットとタスク割り当てをクリーンアップします。

---

## 5. 使用例 (Usage Example)

リモートサーバーに接続するには、Tclコンソールで以下のコマンドを実行します：

```tcl
# 1. リモートサーバーの設定と事前共有キーをNVSに保存する
nvs_set bitun_rem_ip "your-server-domain.com"
nvs_set bitun_rem_port "8888"
nvs_set bitun_psk "abcdefghijklmnopqrstuvwxyz123456"

# 2. トンネルプロキシを起動する
bitun_start

# 3. これにより、ローカルネットワーク内のデバイスから、ESP32の IP:1080 をSOCKS5プロキシとして設定し、セキュアなデータ転送を行えます。

# 4. 必要に応じてトンネルを停止する
bitun_stop
```

---

## 6. Web Shell ウェブコントロールパネル (Web Shell Console)

本プロジェクトは、クロスプラットフォームデバッグや動的構成を非常に容易にするために、WebSocketをベースにした対話型のWeb Tclコンソールを内蔵しています。

*   **デフォルトのWi-Fi接続設定**:
    *   **SSID**: `"testzzzz"`
    *   **パスワード**: `"11111111"`
*   **アクセス方法**:
    1. ESP32がWi-Fiルーターに接続されると、シリアルモニターに割り当てられたローカルIPアドレス（例：`192.168.1.100`）が表示されます。
    2. 同じローカルネットワーク内のデバイスからブラウザを開き、`http://<ESP32_IP>/` にアクセスすると、組み込みの対話型ウェブコンソール画面が表示されます。
    3. 入力欄に任意のTclコマンドを入力してEnterキーを押すと、コマンドがWebSocket（`/ws`）経由でリアルタイムにESP32へ送信され、実行結果がウェブページ上に非同期ストリーミングとして即座に描画されます。

---

## 7. Windows 一クリック書き込みの仕組み (Windows Flashing Principle)

本プロジェクトは、`tools/flash/` 内にWindowsプラットフォーム向けの一クリック書き込みパッケージを同梱しています。`flash.bat` の動作原理とワークフローは以下の通りです：

1.  **書き込みツールチェーンの自動解凍**：
    `flash.bat` の実行時に、スクリプトはローカルに `esptool-v4.11.0-windows-amd64` フォルダが存在するかどうかを確認します。見つからない場合、Windowsに組み込まれている **PowerShell** を呼び出して `Expand-Archive` コマンドを実行し、同ディレクトリ内の `esptool-v4.11.0-windows-amd64.zip` を動的に自動解凍します。
2.  **シリアルポート構成の永続化**：
    同じディレクトリ内の `flashcom.txt` から最後に使用したCOMポート（デフォルトは `COM3`）を読み取ります。ユーザーがプロンプトで新しいポート（例：`COM4`）を入力した場合、スクリプトはそれを自動的に `flashcom.txt` に書き込み保存するため、次回以降の再入力を省略できます。
3.  **マルチパーティションへの直接アドレス書き込み**：
    解凍された `esptool.exe` は、完全なスタンドアロンのコマンドライン書き込みツールとして動作します。バッチスクリプトは、ローカルのESP-IDFコンパイルスイート全体を介さずに、ビルドで生成されたバイナリを直接かつ正確なフラッシュオフセットを指定して書き込みます：
    *   `0x0` -> `../../build/bootloader/bootloader.bin` (セカンドステージブートローダ)
    *   `0x8000` -> `../../build/partition_table/partition-table.bin` (フラッシュのパーティション配置を規定するパーティションテーブル)
    *   `0x10000` -> `../../build/ESP32_ports.bin` (BareTclとBiTunを統合したメインアプリケーションファームウェア)

---

## 8. ライセンス (License)

本プロジェクトは **[Apache License 2.0](LICENSE)** に基づいてオープンソースとして提供されています。  
詳細は、本リポジトリのルートにある `LICENSE` ファイルをご参照ください。


